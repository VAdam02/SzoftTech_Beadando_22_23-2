using System.Collections;
using System.Collections.Generic;
using System;
using UnityEngine;

namespace Persistance
{
    public class GameDataException : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            throw new Exception();
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}

